@page "/"
@using BlazorApp.Models;

<PageTitle>Index</PageTitle>

<div class="row">
    <div class="col-6">
        <div class="card">
            <div class="card-body">
                <button @onclick="AddSneaker" class="btn btn-primary">Tambah Sneaker</button>
                <button @onclick="AddSendal" class="btn btn-success">Tambah Sendal</button>
                <button @onclick="AddSepatuKulit" class="btn btn-warning">Tambah Sepatu Kulit</button>
                <button @onclick="AddPentofel" class="btn btn-danger">Tambah Pantofel</button>
            </div>
        </div>
    </div>
    <div class="col-6">
        <div class="card">
            <div class="card-body">
                <h3>Rak Sepatu</h3>
                <table class="table table-bordered">
                    <tr>
                        <th>Sepatu</th>
                        <th>Aksi</th>
                    </tr>
                    @if (listSepatu != null)
                    {
                        @foreach (var item in listSepatu)
                        {
                            <tr>
                                <td>@item.JenisSepatu</td>
                                @*<td><button @onclick="(() => DeleteList(item.JenisSepatu))" class="btn btn-danger">Delete</button></td>*@
                            </tr>
                        }
                    }
                </table>
            </div>
        </div>
    </div>
</div>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />

@code{
    //KartuRemi asSekop;
    //int angka = 0;
    //string angkas = "";

    //List<KartuRemi> listRemi;
    List<Sepatu> listSepatu;
    int index = 0;

    protected override void OnInitialized()
    {
        //// contructtor
        //KartuRemi asSekop = new KartuRemi(KembangRemi.Sekop, AngkaRemi.As);
        //// casting
        //angka = (int) asSekop.Angka;
        //angkas = asSekop.Angka.ToString();

        //listRemi = new List<KartuRemi>();
        //listRemi.Add(new KartuRemi(KembangRemi.Tempe, AngkaRemi.As));
        //listRemi.Add(new KartuRemi(KembangRemi.Love, AngkaRemi.As));

        //KartuRemi asKeriting =
        //    new KartuRemi(KembangRemi.Keriting, AngkaRemi.As);

        //listRemi.Add(asKeriting);

        //int indexAsKeriting = listRemi.IndexOf(asKeriting);
        //bool adaAsKeriting = listRemi.Contains(asKeriting);

        //listRemi.Remove(asKeriting);

        // ------------------------------------------  

        //listSepatu = new List<Sepatu>()
        //{
        //    new Sepatu(JenisSepatu.Sneaker),
        //    new Sepatu(JenisSepatu.Sendal)
        //};
        listSepatu = new List<Sepatu>();
    }

    public void AddSneaker()
    {
        listSepatu.Add(new Sepatu(JenisSepatu.Sneaker));
    }

    public void AddSendal()
    {
        listSepatu.Add(new Sepatu(JenisSepatu.Sendal));
    }

    public void AddSepatuKulit()
    {
        listSepatu.Add(new Sepatu(JenisSepatu.SepatuKulit));
    }

    public void AddPentofel()
    {
        listSepatu.Add(new Sepatu(JenisSepatu.Pentofel));
    }

    public void DeleteList(Sepatu toDelete)
    {
        listSepatu.Remove(toDelete);
    }
}